import type { WineBatch } from '@/lib/types/types';

/**
 * Deliberate no-op until named Storage Vessel effect rules are approved.
 */
export function applyStorageVesselEffects(batch: WineBatch): WineBatch {
  return batch;
}
