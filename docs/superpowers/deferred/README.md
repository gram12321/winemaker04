# Deferred design records

These documents describe systems that are intentionally not active in the current mainline, or broader ideas whose implemented slice is deliberately smaller than the original design.

They are preserved for lineage and possible reactivation. Moving a record back into `plans/` requires an explicit product decision and a fresh code/status review.
